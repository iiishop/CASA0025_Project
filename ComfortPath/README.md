# ComfortPath

Project structure:

- `backend/`: Python + FastAPI + LAEI 2022 raster tile services
- `frontend/`: Vue + Mapbox frontend application

Current implementation status:
- backend air quality layer MVP scaffold completed
- frontend Vue + Mapbox air quality viewer scaffold completed

Quick start:

1) Start backend (see `backend/README.md`)
2) Start frontend (see `frontend/README.md`)
